import tkinter as tk

#Create an instance of the tkinter class
windows = tk.Tk()
windows.title("GUI")
""""
greeting = tk.Label(
    text='hello tkinter', 
    background='black', 
    foreground='white', 
    width='50',
    height=2
    ) #Creating a label
greeting.pack()

button = tk.Button(
    text="click Me",
    background='Green',
    foreground='White',
    width=10,
    height=2,
    padx=15,
    pady=15

)

button.pack()
EntryVal = tk.Entry(
    fg='yellow',
    bg='black',
    width=50
    )
EntryVal.pack()

label = tk.Label(text="Name") #Creating a label
entry = tk.Entry() #entry field for the label

name = entry.get() #collecting input from the text field
name



label.pack()
entry.pack()


text_box =tk.Text()

text_box.get('1.0')
text_box.pack()"""



windows.mainloop()
